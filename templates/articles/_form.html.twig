{# Partial #}

{{ form_start(form) }} <!-- Creer la balise form -->
{{ form_row(form.nom,
    {'attr' : {
        'placeholder' : "Nom de l'article"},
        'label' : "Titre article"}
) }}

    {{ form_row(form.type,
        {'attr' : {
            'placeholder' : "Type Article"},
            'label' : "Type"}
    ) }}

    {{ form_row(form.redacteur,
        {'attr' : {
            'placeholder' : "Nom du redacteur"},
            'label' : "Redacteur"}
    ) }}

    {{ form_row(form.proteine,
        {'attr' : {
            'placeholder' : "Img"},
            'label' : "Images"}
    ) }}


<input type="submit" value="{{ submitButtonText }}" class="btn btn-success">

{{ form_end(form) }} <!-- Fermer la balise form -->